# =============================================================================
# AIRS-CP Environment Configuration
# Copy this file to .env and configure your settings
# =============================================================================

# -----------------------------------------------------------------------------
# Runtime Configuration
# -----------------------------------------------------------------------------

# Runtime mode: observe (log only) or enforce (block/sanitize)
AIRS_MODE=observe

# Default LLM provider: openai, anthropic, azure, ollama
AIRS_PROVIDER=ollama

# Default model (provider-specific)
# OpenAI: gpt-4, gpt-3.5-turbo
# Anthropic: claude-3-opus-20240229, claude-3-sonnet-20240229
# Azure: your-deployment-name
# Ollama: llama3.2:1b, mistral, codellama
AIRS_MODEL=llama3.2:1b

# Gateway host and port
AIRS_HOST=0.0.0.0
AIRS_PORT=8080

# Kill switch - when true, all requests pass through without security checks
AIRS_KILL_SWITCH=false

# Request timeout in seconds
AIRS_REQUEST_TIMEOUT=120

# -----------------------------------------------------------------------------
# Provider API Keys
# Only configure the providers you plan to use
# -----------------------------------------------------------------------------

# OpenAI
OPENAI_API_KEY=

# Anthropic
ANTHROPIC_API_KEY=

# Azure OpenAI
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Ollama (local LLM server)
# For Docker: use http://host.docker.internal:11434
# For local dev: use http://localhost:11434
OLLAMA_HOST=http://localhost:11434

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# SQLite database path for evidence store
# For Docker: /data/evidence.db (mounted volume)
# For local dev: ./data/evidence.db
AIRS_DB_PATH=./data/evidence.db

# -----------------------------------------------------------------------------
# Logging Configuration (future use)
# -----------------------------------------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR
# AIRS_LOG_LEVEL=INFO

# Log format: json, text
# AIRS_LOG_FORMAT=json
